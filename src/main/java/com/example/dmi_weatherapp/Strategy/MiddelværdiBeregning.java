package com.example.dmi_weatherapp.Strategy;

import java.util.ArrayList;

public class MiddelværdiBeregning implements StrategyPattern
{
/*
    public void Udregning(ArrayList aList){
        do {
            if (calculation > 0){
                calculation--;
                System.out.println("Calculating the Middlevalue");
            }
        } while (calculation > 0);
        System.out.println("Calculation completed....");
        calculation++;
    }


    int[] array = {};
    int total = 0;

    // add each element's value to total
    for (int counter = 0; counter < array.length; counter++) {
    }
    System.out.println("Total of array elements: %d%n", total);
    //Divider med antal i array.
    }
*/
}